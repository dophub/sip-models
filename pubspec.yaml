name: sip_models
description: TürkcellSiparişim uygulamasında kullanılan modeller
version: 1.2.44

environment:
  sdk: ">=2.17.0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  background_json_parser: any

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0

flutter:
